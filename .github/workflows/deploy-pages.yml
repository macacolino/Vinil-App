# Publica o app no GitHub Pages a cada push nas branches abaixo.
# Endereço: https://macacolino.github.io/<nome-do-repositório>/
# O caminho base é calculado a partir do nome do repositório, então renomear
# o repositório no GitHub não quebra nada: o próximo push publica no novo endereço.
name: Publicar no GitHub Pages

on:
  push:
    branches:
      - main
      - claude/sleepy-albattani-3pi081
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Só um deploy por vez; um novo push cancela o anterior.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          VITE_BASE: /${{ github.event.repository.name }}/
          # Chaves do Supabase (Settings > Secrets and variables > Actions). Sem elas o app
          # funciona normalmente, só sem login e sincronização.
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
      # Pages devolve 404.html para caminhos desconhecidos; com uma cópia do
      # index.html, abrir /main/albuns/5 direto no navegador continua funcionando.
      - run: cp dist/index.html dist/404.html
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
